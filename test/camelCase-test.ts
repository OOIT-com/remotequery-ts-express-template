// noinspection SqlResolve
/* tslint:disable:no-console */

import { expect } from 'chai';

describe('Column Name conversion', () => {
  // before(() => {}); // the tests container
  it('camel case dummy', () => {
    expect(Math.PI > 0).equals(true);
  });
});
